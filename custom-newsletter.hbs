<!DOCTYPE html>
<html lang="{{@site.lang}}">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>
    {{@site.title}} - {{meta_title}}
  </title>
  <!--     Fonts and icons     -->
  {{>services/fonts}}
  <!-- Nucleo Icons -->
  <link href="{{asset "css/nucleo-icons.css"}}" rel="stylesheet" />
  <link href="{{asset "css/nucleo-svg.css"}}" rel="stylesheet" />
  <!-- Font Awesome Icons -->
  <link href="{{asset "css/font-awesome.css"}}" rel="stylesheet" />
  <link href="{{asset "css/nucleo-svg.css"}}" rel="stylesheet" />
  {{!-- {{>services/google_fonts}} --}}
  <link rel="stylesheet" type="text/css" href="{{asset "css/argon-design-system.css"}}">
  {{ghost_head}}
</head>

<body>
  <!-- Navbar -->
  {{>navbar}}
  <!-- End Navbar -->
  <div class="page-header">
    <div class="container">
      <div class="row align-items-center text-left">
        <div class="col-lg-5 col-12">
          <h1 class="display-3">{{t "More content with our"}}<span class="text-primary"> {{t "Newsletter"}}</span></h1>
          <p class="lead pb-4">{{t "We aim high at being focused on building relationships with our community.
            Using our creative gifts drives this foundation."}}</p>
          <form data-members-form="subscribe" class="row row-input subscribe-form">
            <div class="col-sm-8 col-12">
              <input data-members-email type="email" class="form-control" name="email" id="signupSrEmail"
                placeholder="{{t "Your email address"}}" aria-label="Your email" required>
            </div>
            <div class="col-sm-4 col-12 pl-lg-0">
              <button type="submit" class="btn btn-primary btn-block">{{t "Subscribe"}}</button>
            </div>
            {{>members_subscribe_alerts}}
            <div class="subscribe-success">
              <h4>{{t "Great!"}}</h3>
                {{t "Check your inbox and click the link to confirm your subscription"}}
                <br>
            </div>
          </form>
        </div>
        <div class="col-lg-7 col-12 pl-0">
          <img class="ml-lg-5" src="{{asset "./img/bg6-2.svg"}}" width="100%">
        </div>
      </div>
    </div>
  </div>
  {{>small_footer}}
  {{ghost_foot}}
</body>

</html>